services:
  mysql:
    image: mysql:8.4
    environment:
      MYSQL_DATABASE: estapar
      MYSQL_ROOT_PASSWORD: root
      MYSQL_USER: estapar
      MYSQL_PASSWORD: estapar
    ports:
      - "3306:3306"
    volumes:
      - mysql_data:/var/lib/mysql

volumes:
  mysql_data:
