spring:
  datasource:
    url: jdbc:mysql://localhost:3306/estapar
    username: estapar
    password: estapar
  jpa:
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
  flyway:
    enabled: true

server:
  port: 8080
